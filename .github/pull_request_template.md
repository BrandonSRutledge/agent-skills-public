## Summary

<!-- What changed and why (1–5 sentences). -->

## Linked issue

<!-- owning-repo#N. Child PRs must not use Fixes/Closes on a [TRACK] parent. -->

## Gate

`PR_TEMPLATE_GATE_FIELD_OPS484`

Exactly one. Fail closed to `gate:human` if unsure. CoS stamps class on `gate:cos` work.

- [ ] `gate:human`
- [ ] `gate:cos`

`TRACK_CLOSE_KEYWORD_NEVER_BEFORE_HASH`: never put close/closes/fixes/resolves immediately before `#N` for a TRACK. Safe: `TRACK 469 stays open` (no hash).

## Checks

- [ ] No secrets, tokens, or license files in this PR
- [ ] Local validation run when applicable
- [ ] Acceptance criteria on the work issue updated
