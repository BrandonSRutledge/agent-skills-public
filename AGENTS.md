# Agent instructions — agent-skills-public

- This repository is **PUBLIC**. Assume anything committed is world-readable forever.
- Only create skills with `classification: PUBLIC`.
- Never add INTERNAL/SENSITIVE content, secrets, or private hostnames.
- For internal/sensitive work, use `agent-skills-private` (org access required).
